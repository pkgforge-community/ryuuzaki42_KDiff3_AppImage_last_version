app: KDiff3

ingredients:
  dist: resolute 
  sources:
    - deb http://archive.ubuntu.com/ubuntu/ resolute main universe
  packages:
    - libproxy

script:
  #- find . | grep ".svg"
  #- find . | grep ".desktop"
  - pwd
  - ls -lah
  - ls -lah ../
  - ls -lah ../../
  - cp ../../README.md .
