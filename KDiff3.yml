app: KDiff3

ingredients:
  dist: plucky 
  sources:
    - deb http://archive.ubuntu.com/ubuntu/ plucky main universe

script:
  #- find . | grep ".svg"
  #- find . | grep ".desktop"
  - pwd
  - ls -lah
  - ls -lah ../
  - ls -lah ../../
  - cp ../../README.md .
