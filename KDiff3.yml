app: KDiff3

ingredients:
  dist: universe
  sources:
    - deb http://archive.ubuntu.com/ubuntu/ universe focal main

  ppas:
    #- blaze/kf5
    #- source/kdiff3

script:
  #- find . | grep ".svg"
  #- find . | grep ".desktop"
  #- pwd
  #- ls -lah
  #- ls -lah ../
  - ls -lah ../../
  - cp ../../README.md .
