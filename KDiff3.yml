app: KDiff3

ingredients:
  dist: focal
  packages:
    - kdiff3=1.9.5
  sources:
    - deb http://archive.ubuntu.com/ubuntu/ focal main universe
  ppas:
    - blaze/kf5

script:
  #- find . | grep ".svg"
  #- find . | grep ".desktop"
  #- pwd
  #- ls -lah
  #- ls -lah ../
  - ls -lah ../../
  - cp ../../README.md .
