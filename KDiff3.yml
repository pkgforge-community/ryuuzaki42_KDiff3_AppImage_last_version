app: KDiff3

ingredients:
  dist: focal
  #packages:
  #  - kdiff3=1.10.7
  sources:
    - deb http://archive.ubuntu.com/ubuntu/ noble-proposed restricted main multiverse universe
  ppas:
    - blaze/kf5

script:
  #- find . | grep ".svg"
  #- find . | grep ".desktop"
  #- pwd
  #- ls -lah
  #- ls -lah ../
  - ls -lah ../../
  - cp ../../README.md .
