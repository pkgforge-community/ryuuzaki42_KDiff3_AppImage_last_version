app: KDiff3

ingredients:
  dist: mantic 
  sources:
    - deb http://archive.ubuntu.com/ubuntu/ mantic main universe

script:
  #- find . | grep ".svg"
  #- find . | grep ".desktop"
  - pwd
  - ls -lah
  - ls -lah ../
  - ls -lah ../../
  - cp ../../README.md .
