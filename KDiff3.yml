app: KDiff3

ingredients:
  dist: resolute 
  sources:
    - deb http://archive.ubuntu.com/ubuntu/ resolute main universe
  packages:
    - libproxy1v5

script:
  #- find . | grep ".svg"
  #- find . | grep ".desktop"
  - pwd
  - ls -lah
  - ls -lah ../
  - ls -lah ../../
  - cp ../../README.md .
