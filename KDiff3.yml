app: KDiff3

ingredients:
  dist: oracular 
  sources:
    - deb http://archive.ubuntu.com/ubuntu/ oracular main universe

script:
  #- find . | grep ".svg"
  #- find . | grep ".desktop"
  - pwd
  - ls -lah
  - ls -lah ../
  - ls -lah ../../
  - cp ../../README.md .
