app: KDiff3
# Test 1

ingredients:
  dist: stonking 
  sources:
    - deb http://archive.ubuntu.com/ubuntu/ stonking main universe
  packages:
     - libproxy-tools libproxy1v5 libproxy-modules
  #  - libproxy1v5 # Error libpxbackend-1.0.so not found
  #  - libselinux # to find
  #  - find # to find . -name '*.desktop'

script:
  #- find . | grep ".svg"
  #- find . | grep ".desktop"
  - pwd
  - ls -lah
  - ls -lah ../
  - ls -lah ../../
  - cp ../../README.md .
