app: KDiff3

ingredients:
  dist: noble 
  sources:
    - deb http://archive.ubuntu.com/ubuntu/ noble main universe

script:
  #- find . | grep ".svg"
  #- find . | grep ".desktop"
  - pwd
  - ls -lah
  - ls -lah ../
  - ls -lah ../../
  - cp ../../README.md .
